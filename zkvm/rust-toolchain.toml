# Host-side toolchain for the zkvm workspace only. risc0-zkvm 3.x needs a
# recent rustc. The guest itself is compiled by the rzup-managed risc0
# toolchain regardless of this file.
[toolchain]
channel = "stable"
