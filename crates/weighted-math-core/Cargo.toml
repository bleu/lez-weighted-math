[package]
name = "weighted-math-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Fixed-point weighted-pool math kernel (no_std, no RISC0 deps yet)."

[dependencies]
