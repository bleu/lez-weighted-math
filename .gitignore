# Rust / Cargo
/target/
/zkvm/target/
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/coverage/
**/*.rs.bk
*.pdb

# Cargo.lock is kept for the workspace (binaries/reproducible builds).
# Remove the next line if this ever becomes a library-only publish.
# /Cargo.lock

# Criterion benchmark output
/target/criterion/

# Process residue kept locally, not published
/docs/archive/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
